<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" type="image/png" href="images/new_logo.png" />
    <script rel="javascript" src="../assets/js/jquery.js"></script>
    <title>Treatment simulation</title>
    <style>
        html,
        body {
            padding: 0;
            margin: 0;
        }
        
        iframe {
            width: 100%;
            height: 100vh;
            border: 0;
            max-height: 100vh;
        }
        
        @media screen and (max-width:767px) {
            iframe {
                max-height: 92vh;
            }
        }
        
        #profile-pic {
            border-radius: 50%;
            border: #000 2px solid;
            position: absolute;
            z-index: 2;
            top: 15px;
            left: calc(50% - 50px);
            width: 100px;
            height: 100px;
        }
        
        #profile-pic2 {
            border-radius: 0%;
            position: fixed;
            z-index: 1;
            top: 20px;
            left: calc(50% - 250px);
            width: 500px;
            height: 100px;
        }
        
        #simulationIframe {
            max-height: 100vh;
            margin-top: -30px;
        }
        
        h1 {
            position: fixed;
            top: 50%;
            left: calc(50% - 70px);
            text-align: center;
        }
    </style>
</head>

<body>
    <div id="profile_pic">

    </div>

    <div id="simulationIframe">

    </div>

    <script>
        $(document).ready(function() {

            let url = ''
            let source_company = 1;

            try {
                url = /\?source_company=(.*)/.exec(document.location.search)[1]
                // source_company = /\?source_company=(.*)&url=/.exec(document.location.search)[1][0]
            } catch (err) {

            }

            if (url == '' || !url) {
                url = /\?url=(.*)/.exec(document.location.search)[1]
            }

            if (!url) {
                $('#simulationIframe').html('<h1>No Simulation</h1>')
            }

            // let result = /\?mlink=https:\/\/www.klineportal.com(.*)/.exec(url);
            let result = /\?mlink=.*/.exec(url);


            // if (source_company == 1) { //dca logo
            //     $('#profile_pic').html(`<img src="images/simulation_link/dca.jpg" id="profile-pic"></img>`)
            // }
            //  else if (source_company == 2) { // dcx logo
            //     $('#profile_pic').html(`<img src="images/simulation_link/dcx.jpg" id="profile-pic"></img>`)
            // } else if (source_company == 3) { // wondersmile logo
            //     $('#profile_pic').html(`<img src="images/simulation_link/drws.jpg" id="profile-pic"></img>`)
            // } else if (source_company == 4) { // kline-apac logo
            //     $('#profile_pic').html(`<img src="images/simulation_link/kline-apac.jpg" id="profile-pic"></img>`)
            // } else if (source_company == 5) { // DCAP logo
            //     $('#profile_pic').html(`<img src="images/simulation_link/dcap.png" id="profile-pic"></img>`)
            // } else if (source_company == 6) { // FLX logo
            //     $('#profile_pic').html(`<img src="images/simulation_link/flx.png" id="profile-pic"></img>`)
            // } else { // no logo
            //     $('#profile_pic').html(`<h1 id="profile-pic2"></h1>`)
            // }


            if (result) {
                $('#simulationIframe').html(`<iframe src="https://onyx.orthorobot.com/webviewer/main.html` + result[0] + `" border="0"></iframe>`)
            } else {
                $('#simulationIframe').html(`<iframe src="https://drclearaligners.xyz/onyx/Viewer/show.html` + result[0] + `" border="0"></iframe>`)
            }
        })
    </script>
</body>

</html>